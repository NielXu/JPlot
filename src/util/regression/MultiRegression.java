package util.regression;

/**
 * Multiple Linear Regression, try to find best fit linear function with given matrices.
 * The metrices should be in the follwing format:<br>
 * |x0,x0,x0,x0|<br>
 * |x1,x1,x1,x1| = X<br>
 * |x2,x2,x2,x2|<br>
 * <br>
 * |y0|<br>
 * |y1| = Y<br>
 * |y2|<br>
 * It means that the X matrix can have multiple columns.
 * And each row in X will produce a y value, therefore, the y matrix should alawys contain
 * only one column. Formula:<br>
 * <center>y = b + x0b0 + x1b1 + x2b2 + ... +xnbn = y = bX</center>
 * @author Daniel Xu
 *
 */
public class MultiRegression {

}
